apiVersion: 1

providers:
  - name: "warehouse"
    orgId: 1
    folder: "Warehouse"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
